<Window x:Class="FloridaLotteryApp.ThirdAnalysisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FloridaLotteryApp"
        Title="Tercer Análisis"
        Height="900"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding AnalysisCards}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Tarjeta para cada resultado del tercer análisis -->
                            <Border CornerRadius="16"
                                    BorderBrush="Black"
                                    BorderThickness="2"
                                    Padding="14"
                                    Margin="0,0,0,14"
                                    Background="#FFF7F7F7"
                                    Loaded="CardBorder_Loaded">
                                
                                <Grid>
                                    <!-- Canvas para conectar dígitos Pick3 -->
                                    <Canvas x:Name="Pick3LinksCanvas"
                                            IsHitTestVisible="False"
                                            Panel.ZIndex="2"/>
                                    
                                    <!-- Canvas para conectar dígitos NextPick3 -->
                                    <Canvas x:Name="NextPick3LinksCanvas"
                                            IsHitTestVisible="False"
                                            Panel.ZIndex="2"/>

                                    <Canvas x:Name="Pick3Row3ToPick4Row4LinksCanvas"
                                            IsHitTestVisible="False"
                                            Panel.ZIndex="2"/>
                                            
                                    <Canvas x:Name="Pick3Row4ToPick4Row3LinksCanvas"
                                            IsHitTestVisible="False"
                                            Panel.ZIndex="2"/>   


                                    <Canvas x:Name="Pick3Row1ToPick4Row2LinksCanvas"
                                        IsHitTestVisible="False"
                                        Panel.ZIndex="2"/>
                                        
                                    <Canvas x:Name="Pick3Row2ToPick4Row1LinksCanvas"
                                        IsHitTestVisible="False"
                                        Panel.ZIndex="2"/>



                                    <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,8,8,0"
                                        Panel.ZIndex="3">
                                
                                <!-- Botón 1: Análisis de Línea -->
                                <Button x:Name="LineAnalysisButton"
                                        Content="1️⃣"
                                        ToolTip="Análisis 4 Coincidencias"
                                        Width="32"
                                        Height="32"
                                        FontSize="14"
                                        Margin="2,0"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderBrush="#CCCCCC"
                                        BorderThickness="1"
                                        Tag="{x:Static local:AnalysisMode.Opcion1}"
                                        Click="analysisButton1">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="8">
                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8F4FD"/>
                                                    <Setter Property="BorderBrush" Value="#2196F3"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                
                                <!-- Botón 2: Análisis de Posición -->
                                <Button x:Name="PositionAnalysisButton"
                                        Content="2️⃣"
                                        ToolTip="Análisis 3 Coincidencias"
                                        Width="32"
                                        Height="32"
                                        FontSize="14"
                                        Margin="2,0"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderBrush="#CCCCCC"
                                        BorderThickness="1"
                                        Tag="{x:Static local:AnalysisMode.Opcion2}"
                                        Click="analysisButton1"
                                        >
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                CornerRadius="8">
                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F3E5F5"/>
                                                    <Setter Property="BorderBrush" Value="#9C27B0"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>             
                                    
                                    <StackPanel>
                                        <!-- ======= FILA 1: FECHA INTERMEDIA (Resultado Original) ======= -->
                                        <Border Background="White" CornerRadius="10" Padding="10">
                                            <StackPanel>
                                                <!-- Fecha/Tirada -->
                                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                    <TextBlock Text="Fecha:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row1DateText}" FontSize="16" Margin="6,0,18,0"/>
                                                    <TextBlock Text="Tirada:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row1DrawIcon}" FontSize="18" Margin="6,0,0,0"/>
                                                </StackPanel>

                                                <!-- Línea de dígitos -->
                                                <Grid Margin="0,10,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Pick3 (3) -->
                                                    <ItemsControl x:Name="Row1Pick3Items"
                                                                  Grid.Column="0" 
                                                                  ItemsSource="{Binding Row1Pick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Pick4 (4) -->
                                                    <ItemsControl x:Name="Row1Pick4Items" Grid.Column="2" ItemsSource="{Binding Row1Pick4Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="4"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- NextPick3 (3) -->
                                                    <ItemsControl x:Name="Row1NextPick3Items"
                                                                  Grid.Column="4" 
                                                                  ItemsSource="{Binding Row1NextPick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Codificación (6) -->
                                                    <ItemsControl x:Name="Row1CodingItems" Grid.Column="6" ItemsSource="{Binding Row1CodingDigits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="6"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                           VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>
                                            </StackPanel>
                                        </Border>

                                        <!-- ======= FILA NUEVA 1A: CONTADOR CODIFICACIÓN 1-2 ======= -->
                                        <Border Background="Transparent" Height="30" Margin="0,5,0,5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>  <!-- Nuevo: para Pick4 -->
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Contador de Pick4 -->
                                                <TextBlock Grid.Column="1"
                                                        Text="{Binding Pick4MatchesRow1Row2}"
                                                        FontSize="16"
                                                        FontWeight="Bold"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left"
                                                        Margin="0,0,0,0"
                                                        Foreground="Black"
                                                        Padding="10,5"/>
                                                
                                                <!-- Contador de Codificación -->
                                                <TextBlock Grid.Column="2"
                                                        Text="{Binding CodingMatchesRow1Row2}"
                                                        FontSize="16"
                                                        FontWeight="Bold"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left"  
                                                        Margin="350,0,10,0"  
                                                        Foreground="Black"
                                                        Padding="10,5"/>
                                            </Grid>
                                        </Border>

                                        <!-- ======= FILA 2: FECHA MENOR ======= -->
                                        <Border Background="White" CornerRadius="10" Padding="10" Margin="0,0,0,0">
                                            <StackPanel>
                                                

                                                <!-- Línea de dígitos -->
                                                <Grid Margin="0,10,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Pick3 (3) -->
                                                    <ItemsControl x:Name="Row2Pick3Items"
                                                                  Grid.Column="0" 
                                                                  ItemsSource="{Binding Row2Pick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Pick4 (4) -->
                                                    <ItemsControl x:Name="Row2Pick4Items" Grid.Column="2" ItemsSource="{Binding Row2Pick4Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="4"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- NextPick3 (3) -->
                                                    <ItemsControl x:Name="Row2NextPick3Items"
                                                                  Grid.Column="4" 
                                                                  ItemsSource="{Binding Row2NextPick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Codificación (6) -->
                                                    <ItemsControl x:Name="Row2CodingItems" Grid.Column="6" ItemsSource="{Binding Row2CodingDigits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="6"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>

                                                <!-- Fecha/Tirada -->
                                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                    <TextBlock Text="Fecha:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row2DateText}" FontSize="16" Margin="6,0,18,0"/>
                                                    <TextBlock Text="Tirada:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row2DrawIcon}" FontSize="18" Margin="6,0,0,0"/>
                                                </StackPanel>


                                            </StackPanel>
                                        </Border>

                                        <!-- ======= ESPACIO ENTRE GRUPOS ======= -->
                                        <Border Height="40" Background="Transparent"/>

                                        <!-- ======= FILA 3: FECHA MAYOR ======= -->
                                        <Border Background="White" CornerRadius="10" Padding="10" Margin="0,0,0,0">
                                            <StackPanel>
                                                <!-- Fecha/Tirada -->
                                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                    <TextBlock Text="Fecha:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row3DateText}" FontSize="16" Margin="6,0,18,0"/>
                                                    <TextBlock Text="Tirada:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row3DrawIcon}" FontSize="18" Margin="6,0,0,0"/>
                                                </StackPanel>

                                                <!-- Línea de dígitos -->
                                                <Grid Margin="0,10,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Pick3 (3) del resultado -->
                                                    <ItemsControl x:Name="Row3Pick3Items" Grid.Column="0" ItemsSource="{Binding Row3Pick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Pick4 (4) del resultado -->
                                                    <ItemsControl x:Name="Row3Pick4Items" Grid.Column="2" ItemsSource="{Binding Row3Pick4Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="4"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- NextPick3 (3) del resultado -->
                                                    <ItemsControl x:Name="Row3NextPick3Items"
                                                                  Grid.Column="4" 
                                                                  ItemsSource="{Binding Row3NextPick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Codificación (6) del resultado -->
                                                    <ItemsControl x:Name="Row3CodingItems" Grid.Column="6" ItemsSource="{Binding Row3CodingDigits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="6"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                           FontSize="16" FontWeight="Bold"
                                                                           HorizontalAlignment="Center"
                                                                           VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>
                                            </StackPanel>
                                        </Border>

                                        <!-- ======= FILA NUEVA 3A: CONTADOR CODIFICACIÓN 3-4 ======= -->
                                        <Border Background="Transparent" Height="30" Margin="0,5,0,5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>  <!-- Nuevo: para Pick4 -->
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Contador de Pick4 -->
                                                <TextBlock Grid.Column="1"
                                                        Text="{Binding Pick4MatchesRow3Row4}"
                                                        FontSize="16"
                                                        FontWeight="Bold"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left"
                                                        Margin="0,0,0,0"
                                                        Foreground="Black"
                                                        Padding="10,5"/>

                                                <!-- Contador de Codificación -->
                                                <TextBlock Grid.Column="2"
                                                        Text="{Binding CodingMatchesRow3Row4}"
                                                        FontSize="16"
                                                        FontWeight="Bold"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left"  
                                                        Margin="350,0,10,0"
                                                        Foreground="Black"
                                                        Padding="10,5"/>
                                                
                                                
                                            </Grid>
                                        </Border>

                                        <!-- ======= FILA 4: REPETIR FECHA MENOR ======= -->
                                        <Border Background="White" CornerRadius="10" Padding="10" Margin="0,0,0,0">
                                            <StackPanel>
                                                

                                                <!-- Línea de dígitos -->
                                                <Grid Margin="0,10,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="26"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Pick3 (3) -->
                                                    <ItemsControl x:Name="Row4Pick3Items"
                                                                  Grid.Column="0" 
                                                                  ItemsSource="{Binding Row4Pick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Pick4 (4) -->
                                                    <ItemsControl x:Name="Row4Pick4Items" Grid.Column="2" ItemsSource="{Binding Row4Pick4Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="4"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- NextPick3 (3) -->
                                                    <ItemsControl x:Name="Row4NextPick3Items"
                                                                  Grid.Column="4" 
                                                                  ItemsSource="{Binding Row4NextPick3Digits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="3"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <!-- Codificación (6) -->
                                                    <ItemsControl x:Name="Row4CodingItems" Grid.Column="6" ItemsSource="{Binding Row4CodingDigits}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <UniformGrid Rows="1" Columns="6"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Width="34" Height="34" CornerRadius="17"
                                                                        BorderThickness="2" 
                                                                        Background="{Binding Bg}"
                                                                        Margin="4,0">
                                                                    <TextBlock Text="{Binding Value}"
                                                                               FontSize="16" FontWeight="Bold"
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>

                                                <!-- Fecha/Tirada -->
                                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                    <TextBlock Text="Fecha:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row4DateText}" FontSize="16" Margin="6,0,18,0"/>
                                                    <TextBlock Text="Tirada:" FontSize="16" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Row4DrawIcon}" FontSize="18" Margin="6,0,0,0"/>
                                                </StackPanel>


                                            </StackPanel>
                                        </Border>
                                    
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>