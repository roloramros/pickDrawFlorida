<Window x:Class="FloridaLotteryApp.AnalisisGuardadosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Analisis Guardados"
        Height="550"
        Width="840"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#EEF4F8"
        MinHeight="400"
        MinWidth="600">

    <Window.Resources>
        <LinearGradientBrush x:Key="WindowBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F5F9FC" Offset="0"/>
            <GradientStop Color="#E8F0F6" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2B8CFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.78"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox" x:Key="StyledComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C8D6E5"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <ContextMenu x:Key="FolderRowContextMenu">
            <MenuItem Header="Abrir" Click="Abrir_Click"/>
            <MenuItem Header="Borrar" Click="Borrar_Click"/>
        </ContextMenu>
    </Window.Resources>

    <Grid Margin="18" Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#FCFEFF"
                CornerRadius="16"
                Padding="20"
                Margin="0,0,0,15"
                BorderBrush="#D8E4EF"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <TextBlock Text="Analisis Guardados"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#1B2A3A"/>
                    <TextBlock Text="Click derecho sobre cada fila para eliminar o abrir la carpeta"
                               Margin="0,6,0,0"
                               FontSize="13"
                               Foreground="#61758A"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            VerticalAlignment="Center">
                    <TextBlock Text="Tipo de analisis"
                               Margin="0,0,0,8"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#395066"/>
                    <ComboBox x:Name="AnalisisComboBox"
                              Style="{StaticResource StyledComboBox}"
                              SelectionChanged="AnalisisComboBox_SelectionChanged"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Background="#FCFEFF"
                CornerRadius="16"
                BorderBrush="#D8E4EF"
                BorderThickness="1">
            <DataGrid x:Name="FoldersDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="None"
                      BorderThickness="0"
                      RowHeight="46"
                      FontSize="13"
                      FontWeight="SemiBold"
                      AlternatingRowBackground="#F6FAFD"
                      Background="Transparent"
                      Foreground="#213142"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      HorizontalScrollBarVisibility="Disabled"
                      SelectionChanged="FoldersDataGrid_SelectionChanged"
                      PreviewMouseRightButtonDown="FoldersDataGrid_PreviewMouseRightButtonDown">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Folder"
                                        Binding="{Binding Folder}"
                                        Width="1.2*"/>
                    <DataGridTextColumn Header="Descripción"
                                        Binding="{Binding Label}"
                                        Width="*"/>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="ContextMenu" Value="{StaticResource FolderRowContextMenu}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="#E6EDF4"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF7FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2F6FDF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F2F7FB"/>
                        <Setter Property="Foreground" Value="#4B6178"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Height" Value="42"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderBrush" Value="#DDE7F0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,4"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2F6FDF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
            </DataGrid>
        </Border>

        <Border Grid.Row="2"
                Margin="0,15,0,0"
                Background="#FCFEFF"
                BorderBrush="#D8E4EF"
                BorderThickness="1"
                CornerRadius="16"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="ContadorTextBlock"
                           Grid.Column="0"
                           Text="0 folders"
                           VerticalAlignment="Center"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#5B7288"/>

                <Button Grid.Column="2"
                        Content="Cerrar"
                        Style="{StaticResource ActionButton}"
                        Click="Cerrar_Click"
                        Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>


