<Window x:Class="FloridaLotteryApp.AnalysisResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resultadfyjos del análisis"
        Height="550" Width="720"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtHeader"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <ListView Grid.Row="1" ItemsSource="{Binding Items}" FontSize="14">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" Width="40">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsChecked}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Fecha" DisplayMemberBinding="{Binding Date}" Width="120"/>
                    <GridViewColumn Header="Tirada" DisplayMemberBinding="{Binding DrawTime}" Width="60"/>
                    <GridViewColumn Header="Pick3" DisplayMemberBinding="{Binding Pick3}" Width="90"/>
                    <GridViewColumn Header="Pick4" DisplayMemberBinding="{Binding Pick4}" Width="90"/>
                    <GridViewColumn Header="Próxima Tirada" DisplayMemberBinding="{Binding NextPick3}" Width="110"/>
                    <GridViewColumn Header="Codificación" DisplayMemberBinding="{Binding Coding}" Width="110"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
