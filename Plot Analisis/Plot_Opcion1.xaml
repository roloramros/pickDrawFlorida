<Window x:Class="FloridaLotteryApp.Plot_Opcion1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Plot: Opción 1" 
        Height="740" 
        Width="1150"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize">
    
    <Window.Resources>
        <!-- Estilo para bordes de dígitos -->
        <Style TargetType="Border" x:Key="DigitBorder">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    
        <!-- Estilo para texto de dígitos -->
        <Style TargetType="TextBlock" x:Key="DigitText">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Estilo para dígitos resaltados (en la tirada) -->
        <Style TargetType="Border" x:Key="HighlightedDigitBorder" BasedOn="{StaticResource DigitBorder}">
        </Style>

        <!-- Estilo para dígitos sugeridos -->
        <Style TargetType="Border" x:Key="SuggestedDigitBorder" BasedOn="{StaticResource DigitBorder}">
        </Style>

        <DataTemplate x:Key="ColoredDigitTemplate">
            <Border Style="{StaticResource DigitBorder}" Background="{Binding Background}">
                <TextBlock Style="{StaticResource DigitText}" Text="{Binding Value}"/>
            </Border>
        </DataTemplate>
    </Window.Resources>

   
    
    <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Canvas superpuesto para las líneas de conexión -->
            <Canvas x:Name="ConnectionCanvas" 
                    Grid.Row="2"
                    IsHitTestVisible="False"
                    Panel.ZIndex="10"
                    Margin="15,0,15,0"/>

            <!-- ============================================ -->
            <!-- SECCIÓN SUPERIOR: TABLA DE PATRONES (4 Columnas) -->
            <!-- ============================================ -->
            <Border Grid.Row="0" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="15"
                    BorderThickness="1" >
                <Grid>
                    

                    <DataGrid x:Name="PatternsTable"
                            Grid.Row="0"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            CanUserReorderColumns="False"
                            CanUserResizeRows="False"
                            CanUserSortColumns="False"
                            HeadersVisibility="Column"
                            GridLinesVisibility="Vertical"
                            IsReadOnly="True"
                            SelectionMode="Single"
                            SelectionUnit="FullRow"
                            RowHeaderWidth="0"
                            MaxHeight="250"
                            HorizontalScrollBarVisibility="Auto"
                            VerticalScrollBarVisibility="Auto"
                            FontSize="14">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F2F2F2"/>
                                <Setter Property="BorderBrush" Value="#D0D0D0"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#E6E6E6"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#0000FF"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Patron de referencia" Binding="{Binding ReferenceNumber}" Width="*"/>
                            <DataGridTextColumn Header="Coincidencias" Binding="{Binding MatchNumber}" Width="*"/>
                            <DataGridTextColumn Header="Patrones similares" Binding="{Binding SimilarNumber}" Width="*"/>
                            <DataGridTextColumn Header="Coincidencias" Binding="{Binding SimilarMatchNumber}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- ============================================ -->
            <!-- SECCIÓN INFERIOR: 4 FILAS DE INFORMACIÓN -->
            <!-- ============================================ -->
            <Border Grid.Row="2" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="15" 
                    BorderThickness="1" 
                    BorderBrush="#CCCCCC">
                <StackPanel>
            
                    <!-- FILA 1: Tirada seleccionada (datos del click) -->
                    <Border CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                    
                            <!-- Lado Izquierdo: Dígitos de la tirada -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Pick 3 -->
                                <ItemsControl x:Name="Row1_Pick3Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <!-- Pick 4 -->
                                <ItemsControl x:Name="Row1_Pick4Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <!-- Pick3 siguiente -->
                                <ItemsControl x:Name="Row1_Pick3SiguienteDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource HighlightedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="30"/>
                            
                                <!-- Ícono de tirada -->
                                <TextBlock x:Name="Row1_DrawIcon" 
                                        Text="🌙" 
                                        FontSize="24" 
                                        VerticalAlignment="Center"/>
                            </StackPanel>
                    
                            <!-- Centro: Fecha -->
                            <TextBlock Grid.Column="1" 
                                    x:Name="Row1_DateText"
                                    FontWeight="Bold" 
                                    FontSize="14" 
                                    VerticalAlignment="Center"
                                    Margin="20,0"/>
                                        
                            <!-- Lado Derecho: Dígitos adicionales (sugeridos/disponibles) -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ItemsControl x:Name="Row1_AdditionalDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource SuggestedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </Border>
        
                    <!-- FILA 2: Segunda fila (misma estructura) -->
                    <Border CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                    
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <ItemsControl x:Name="Row2_Pick3Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <ItemsControl x:Name="Row2_Pick4Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <ItemsControl x:Name="Row2_FireballDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource HighlightedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="30"/>
                            
                                <TextBlock x:Name="Row2_DrawIcon" 
                                        Text="☀️" 
                                        FontSize="24" 
                                        VerticalAlignment="Center"/>
                            </StackPanel>
                    
                            <TextBlock x:Name="Row2_DateText"
                                    Grid.Column="1" 
                                    Text="Fecha" 
                                    FontWeight="Bold" 
                                    FontSize="14" 
                                    VerticalAlignment="Center"
                                    Margin="20,0"/>
                    
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ItemsControl x:Name="Row2_AdditionalDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource SuggestedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </Border>
            
                    <!-- 🔹 LÍNEA FINA ENTRE FILA 2 Y FILA 3 -->
                    <Rectangle Height="1" Fill="#999696" Margin="0,8,0,8"/>

                    <!-- FILA 3: Tercera fila (misma estructura) -->
                    <Border CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                    
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <ItemsControl x:Name="Row3_Pick3Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <ItemsControl x:Name="Row3_Pick4Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <ItemsControl x:Name="Row3_FireballDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource HighlightedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="30"/>
                            
                                <TextBlock x:Name="Row3_DrawIcon" 
                                        Text="🌙" 
                                        FontSize="24" 
                                        VerticalAlignment="Center"/>
                            </StackPanel>
                    
                            <TextBlock x:Name="Row3_DateText"
                                    Grid.Column="1" 
                                    Text="Fecha" 
                                    FontWeight="Bold" 
                                    FontSize="14" 
                                    VerticalAlignment="Center"
                                    Margin="20,0"/>
                    
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ItemsControl x:Name="Row3_AdditionalDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource SuggestedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </Border>
            
                    <!-- FILA 4: Cuarta fila (misma estructura) -->
                    <Border CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                    
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <ItemsControl x:Name="Row4_Pick3Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <ItemsControl x:Name="Row4_Pick4Digits" ItemTemplate="{StaticResource ColoredDigitTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="20"/>
                            
                                <ItemsControl x:Name="Row4_FireballDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource HighlightedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            
                                <TextBlock Text=" " Width="30"/>
                            
                                <TextBlock x:Name="Row4_DrawIcon" 
                                        Text="☀️" 
                                        FontSize="24" 
                                        VerticalAlignment="Center"/>
                            </StackPanel>
                    
                            <TextBlock x:Name="Row4_DateText"
                                    Grid.Column="1" 
                                    Text="Fecha" 
                                    FontWeight="Bold" 
                                    FontSize="14" 
                                    VerticalAlignment="Center"
                                    Margin="20,0"/>
                    
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ItemsControl x:Name="Row4_AdditionalDigits">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource SuggestedDigitBorder}">
                                                <TextBlock Style="{StaticResource DigitText}" Text="{Binding}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Margin="0,12,0,0" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="ResultsCounterText"
                                Grid.Column="0"
                                Text="0 de 0"
                                VerticalAlignment="Center"
                                FontWeight="SemiBold"
                                FontSize="14"/>

                        <StackPanel x:Name="LoadingPanel"
                                Grid.Column="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                            <TextBlock x:Name="AnalysisProgressText"
                                    Text="Analizando..."
                                    VerticalAlignment="Center"
                                    FontWeight="SemiBold"
                                    Margin="0,0,10,0"/>
                            <ProgressBar x:Name="AnalysisProgressBar"
                                        Width="340"
                                        Height="14"
                                        Minimum="0"
                                        Maximum="1"
                                        Value="0"
                                        VerticalAlignment="Center"/>
                            <Button x:Name="CancelButton"
                                    Content="Cancelar Analisis"
                                    Width="150"
                                    Height="28"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    Click="CancelButton_Click"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
   
</Window>







